# CLAUDE.md — 演讲精读 · talk-deepread

这是云端 routine 的执行说明。你是引擎本身（Opus），**自主完成、不等任何人确认**。当前工作目录已挂载本仓库。

定位：每期挑**一场**演讲（TED，或同类的毕业演讲、冠名讲座、诺贝尔演讲、传世公开演说），**以演讲为入口，把它承载的那个想法讲透**：演讲本身说了什么、它背后压着什么研究和思想、这个想法后来怎么样了。

目标：**读完这一页，读者不必再去看这场演讲，并且比只看过演讲的人知道得多两层**——知道台上没讲的冰山，也知道这个想法十几年后的下场。

**这不是「演讲摘要」，不是「金句合集」，不是「TED 推荐」。**

一场演讲 15–40 分钟、两三千词，本身很薄。所以本站的价值有三分之二不在演讲里：

1. **讲透演讲本身**（它是替代品——读者不用看视频）
2. **冰山下面**：演讲者为了 18 分钟砍掉了什么？他依据的那几项研究实际是怎么做的、样本多大、结论比台上说的窄多少？他更长的书和论文里的完整论证是什么样？
3. **后来的命运**：被证实、部分站住、被推翻、被滥用、还悬着？**这是本站最有辨识度的一节**——播放量榜上好几场演讲的核心结论后来没通过复制实验，而大众记住的仍是台上那个版本。

**要讲得好看**：演讲是表演，有它的节奏、包袱、那个让全场安静下来的时刻。别把它压成要点列表。

---

## 0. 开工第一件事：自己跑 `bash setup.sh`

**每次运行都自己跑 `bash setup.sh`**（几秒），它会重写仓库根目录的 `.preflight`。**不要信任环境自带 setup script 留下的 `.preflight`**——本 routine 可能和博客精读共用环境，那里的探针测的是博客域名，报 OK 不代表 ted.com 能通。

- `egress: OK` + `ted.com: OK` → 照常做。
- `egress: OK` + `ted.com: BLOCKED` → 若本期是 TED 演讲，**不发布**，PushNotification 说明环境白名单缺 `ted.com`，结束。非 TED 演讲照常。
- `egress: BLOCKED` → **立刻停**。不建、不改任何文件，不跑 publish，发一条 PushNotification 说明环境 Network access 需要检查（`Trusted` 档只放行包管理源和 GitHub；`Custom` 档需把演讲与研究来源域名加进 Allowed domains，或改 `Full`），然后结束。**绝不退回凭记忆写。**

## 1. 选题 / 编号

- **严格从 `TOPICS.md` 顶部往下取第一个未做的 slug**。判定已发布：`ls *-talk*.html`（去掉 `.en` 去重）。清单已按「跨模型票数 + 领域交错」排好，别跳序。
- 文件名 `{slug}-talk{N}.html`，**N = 现有最大 N + 1**（写作顺序）。
- **`TOPICS.md` 只读不改**（`publish.sh` 硬卡改动）。全做完 → PushNotification 请 BigCat 续单、**本次不发布**、结束。
- **`[surprise]` 不许跳过**：轮到就写。它存在的理由就是「跟兴趣不够贴」。
- `TOPICS.md` 每条下面那句钩子是**选题时写的线索，不是结论**。读完原文和研究后发现钩子说错了，按你读到的写。

## 2. 动手前：先真读（三层都要读到）

**你对这些演讲几乎都有印象——这正是最危险的地方。** 著名演讲在网上被转述了无数遍，转述版和原文往往不一样（金句被改写、限定语被删、数字被四舍五入）。**本页引用的每一句话、每一个数字，必须来自你本次运行实际读到的文本。**

### 2a. 演讲全文（必须，读不到就不发布）

- **TED 演讲**：`python3 tools/ted_transcript.py <TOPICS.md 里的 transcript URL>`。TED 页面是前端渲染的，WebFetch 经常拿到空壳，**用这个脚本**。脚本退出码 2 = 没抓到逐字稿 → 按本节末尾处理，**不要**改用记忆或二手转述。
- **非 TED**：WebFetch（或 `curl -sSL -A '<浏览器 UA>'`）打开 `TOPICS.md` 给的 transcript 链接；PDF 用 `curl` 下载后 `pdftotext`（没有就 `python3 -c` 读）。
- **从头读到尾**，不是抽段落。记下演讲时长、结构（几个段落、几个故事、在哪里转折）。
- **标 `[第三方转录]` 的**（Munger、DFW、Jobs、Gaiman、Buffett、索尔仁尼琴、施一公）没有官方逐字稿：**再找一份独立来源**（另一个转录站、官方视频页的文字说明、出版的文集节选），**你要引用的句子在两份里对得上才引**；只找到一份就照引，但在「出处」里注明该转录来源。
- **把全文存到临时文件**（`/tmp/transcript.txt`），写作时引语从这个文件里复制，**不要凭印象重打**。

### 2b. 冰山下面（至少读 2 份一手材料）

演讲依据的东西，按优先级：

1. **演讲者自己点名的研究** / 演讲里出现的实验、数据、案例 → 找到**原始论文或报告**（期刊页、PubMed、作者主页 PDF、arXiv），至少读摘要 + 方法 + 主要结果。**要记下的是台上没说的**：样本量、被试是谁、效应多大、原文里的限定条件。
2. **演讲者更长的文字**：同主题的书（出版社页 / 作者放出的章节 / 书评里的引文）、长文、论文。
3. 演讲者其他演讲或访谈里对**同一想法**的更完整表述。

**读不到原文的研究，只报「演讲里提到了 X 研究」这一事实，不复述它的细节。**

### 2c. 后来的命运（至少读 2 份，且至少 1 份独立于演讲者）

WebSearch 按下面这些形状搜，**用英文和演讲者原语言各搜一轮**：

- `{核心主张} replication` / `meta-analysis` / `failed to replicate` / `criticism` / `debunked` / `revisited`
- `{演讲者} responds to critics` / `{演讲者} {年份} update`
- 演讲者本人事后的修正或撤回（很多人写过「十年后回看」）

**要找的是有日期、有出处的事件**：哪一年、谁、做了什么实验或发表了什么反驳、结论是什么、演讲者怎么回应。

**判词分五档**，页面上必须给一个：`站住了` / `部分站住` / `被推翻` / `被滥用`（原意站得住，但被传播成了它没说过的东西）/ `仍悬而未决`。**判词必须由你在 2c 里读到的证据支撑**；证据不足就判「仍悬而未决」并说清缺什么。**不许为了戏剧性判「被推翻」，也不许为了厚道判「站住了」。**

不是每场演讲都有复制危机：Jobs、DFW、蔡元培这类讲的是价值和选择，「命运」写的是**这个想法后来被怎么用、被怎么误读、在今天还成不成立**（例如「follow your passion」被怎样批评）。哲学类演讲写后来的学界回应。

### 读不到就别硬写

演讲全文打不开 / 内容与清单严重不符 → **不发布本期**，PushNotification 报告该 slug 的问题请 BigCat 核实，结束。**绝不能靠记忆编一篇出来。**

## 3. 页面结构

写两个文件：中文 `{slug}-talk{N}.html`、英文 `{slug}-talk{N}.en.html`。**从 `TEMPLATE.html` 复制起步**（样式、modebar、langbar、切换脚本都已就位），删掉所有 `TEMPLATE` 注释。

**每页两种模式**，顶部 `.modebar` 药丸切换，默认**精华版**：

- **精华版（`.mode-pop`）**：~800 字，一篇一口气读完的短文，不是摘要。从演讲里最有画面的一刻开场，讲清核心想法，**最后一段必须交代它后来的命运**——精华版读者也该知道这个想法有没有站住。
- **完整版（`.mode-deep`，`hidden`）**：`.meta-card` + 下面六节。

**六节（顺序即节奏）：**

1. **那一刻** — 从演讲里挑**最具体、最有现场感**的那个瞬间开场：一个道具、一句让全场笑或安静的话、一个数字、一张图。**绝不用「这是一场影响了千万人的演讲」「XX 是著名的 YY」开场。** 判据：第一句能不能让人愣一下。
2. **讲的人，和那一年** — 这人是谁（带一个有画面的细节，不写履历表）；**为什么是那一年、那个场合**：他当时在研究什么、这个想法当时处在什么位置（刚发表？已经火了？没人信？）、台下是谁。3–5 句 + 一段背景。
3. **这场演讲，讲透（主体之一）** — 按演讲自己的推进顺序，把论证从头讲一遍：他从哪儿切入 → 抛出什么问题 → 用什么证据/故事/演示推进 → 在哪里转折 → 落在哪个结论。**是叙述不是罗列**，别给每段套小标题。**至少 2 句原话引语**，挑他说得最好、最像他的句子。读者读完这一节，应该能把这场演讲复述给别人听。
4. **冰山下面（主体之二）** — 台上 18 分钟没装下的东西：研究实际怎么做的、**台上的版本比原始结论宽了多少**、他在书里的完整论证、这个想法在他整个思想里的位置。**这一节最能拉开和「看过演讲的人」的差距**，要具体到样本、效应、限定条件。
5. **后来的命运** — 先放 `.fate` 判词框（五档之一 + 截至年月 + 一两句判词），再按时间讲：发生了什么、谁、证据是什么、演讲者怎么回应、今天学界共识在哪。**有分量的批评者点名**；读不到批评原文就只报书目事实，明说没读过，**不复述没读过的论证**。
6. **出处** — 逐字稿链接 + 本页用到的研究/书/批评，3–6 条，每条一句「它是什么、支撑了本页哪一处」。**这是出处不是导读**，不写「推荐阅读」「从哪篇开始」。**每条链接必须验证能打开。**

需要前置概念时，在第 2 节后加 `<h2>术语表 · Glossary</h2>`（`.glossary` 虚线框）。

**不写「这对你的启发」「谁该看」「怎么看这场演讲」「延伸观看」这类填充节。**

## 4. 写作要求

- **篇幅：中文全页 3000–4500 字，目标 ~3800**（精华版 ~800 + 完整版 ~3000）。
  - 动笔前分配完整版预算：那一刻 ~250 / 讲的人和那一年 ~350 / **演讲讲透 ~900** / **冰山下面 ~750** / **后来的命运 ~600** / 出处 ~150。
  - **没有硬性字数闸门**（姊妹站实测过：硬闸会逼出删「的/了」凑数的行为，成稿更生硬）。真要缩，砍掉一整个次要例子，一次到位。
  - 低于 3000 字通常意味着冰山或命运没挖——回 2b / 2c 补读，别在演讲复述上注水。
- **面向零基础读者**：不认识演讲者、不熟那个领域。术语随名即释，能换大白话就换。
- **引语是生命线**：每句引语必须能在 2a 存下的全文里找到原文，**一个字都不许改**（英文演讲给英文原句 + 中译；中文演讲直接给原句）。**编造或改写某人说过的话，是本站最严重的错误。** 大众流传的「金句版本」和原文不一致时，用原文，可以顺手指出流传版改了什么。
- **数字同理**：演讲里的数字照原文；研究的数字照论文。两者不一致时**两个都报，并点明差异**——这本身常常就是「冰山下面」最好的素材。
- **拿不准就不写**：找不到出处的一律删掉，不用「据说」「他大意是说」蒙混。
- **不神化也不看轻**：著名演讲者多有强烈立场。如实呈现；**不要因为立场与主流不同就加缓冲垫**，也不要因为演讲出名就放过它的漏洞——第 5 节自会处理。
- **腔调**：
  - 可以有节奏、有幽默、直接对读者说话。演讲本身是表演，允许你写出现场感。
  - **不写腰封**：全篇不出现「震撼」「封神」「不可不看」「醍醐灌顶」「值得每个人」这类推荐语。
  - **具体压过抽象**：能写他台上举的那个真例子，就别写「他善于讲故事」。
- **配图按需**：只有演讲涉及有空间结构的东西（规模律曲线、模型框架、实验设计）才配内联 SVG 自绘图 + `figcaption`。**纯思想论述不配图。**
- **中英双语，两版都地道**：两个独立文件，langbar 互链。**英文页除 langbar 的「中文 →」外不得出现汉字**（含 SVG）。例外：中文演讲（蔡元培、施一公）的人名、题目、被引原文可保留中文，但每处挂拼音转写 + 英文释义。
- **中文页术语首次出现补英文**：`中文术语（English）`。人名照原样不译。
- **重点加粗**，克制。

## 5. 版式

以 `TEMPLATE.html` 为准：无衬线、渐变标题、mono 小标签、modebar。**本站配色是青绿 → 暖黄**（`#5ee6a8` → `#e8d46b`，冷暗底 `#0e1311`），模板里已经写好，别改成姊妹站的靛蓝（博客精读）或琥珀（论文精读）。

## 6. 文件约定 / 发布

- 文件放仓库根目录：`{slug}-talk{N}.html` + `{slug}-talk{N}.en.html`。
- **每次运行开头先「对齐」灰行**：扫 `TOPICS.md`，凡是 slug 没有对应页、且 index 里没有灰行的，按清单顺序补一条 `<div class="entry todo">`，插在 `<div class="group" data-todo>` 之后。两个 index 都要补。
- 发布前更新 `index.html`：找到本期演讲的灰色占位行，**原地**改成链接、去掉 `todo` 类；**绝不在末尾 append**。`index.en.html` 同样处理。形状：

  ```html
  <!-- 之前 -->
  <div class="entry todo"><span class="date">—</span><span class="title">Anil Seth — Your brain hallucinates your conscious reality</span><span class="meta">TED · 2017</span></div>
  <!-- 之后 -->
  <a class="entry" href="anil-seth-controlled-hallucination-talk2.html" data-updated="2026-09-14"><span class="date">2026-09-14</span><span class="title">Anil Seth — 一句钩子</span><span class="meta">TED · 2017</span></a>
  ```

  `data-updated` 和 `.date` 都填今天的日期。排序由 index 页尾脚本管，你不用排。
- **index 的 `.title` 是钩子不是题目**：`演讲者 — 一个 clause 点出这场演讲的核心想法（可以带上它的命运）`，中文 ≤ 约 50 字。英文 index 同理写英文钩子。
- **别在 index 里加 ★ 或 surprise 标记**。
- **不要**手动加 `comments.js` / `search.js` / `index-button.js` / `i18n-tts.js` / `lightbox.js`（GitHub Action 自动注入），也别硬写 `← Hub`。
- 用 `./publish.sh` 发布。git：`user.name=BigCat` / `user.email=chengchen0802@gmail.com`。

## 7. 完成后

调用 **PushNotification**（`status:"proactive"`，一行 < 200 字、无 markdown）：
`演讲精读已更新：Amy Cuddy《身体语言塑造你》· 播放量前三的想法，复制实验里没站住 · cissy0802.github.io/talk-deepread`
